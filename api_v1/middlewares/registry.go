package middlewares

import (
	"github.com/labstack/echo/v4"
)

func Middlewares(e *echo.Echo, api *echo.Group) {
	// e.Pre(middleware.RemoveTrailingSlash())
	// e.Use(middleware.Logger())
	// e.Use(middleware.Recover())
}
